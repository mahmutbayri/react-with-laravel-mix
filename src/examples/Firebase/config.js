const config = {
    apiKey: process.env.FIREBASE__API_KEY,
    authDomain: "react-with-laravel-mix.firebaseapp.com",
    databaseURL: "https://react-with-laravel-mix.firebaseio.com"
};

export default config;
